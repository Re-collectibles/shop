name: Update New Listings

on:
  push:
    paths:
      - 'data/*.csv'   # only runs when a CSV is added/changed

jobs:
  generate:
    runs-on: ubuntu-latest
    permissions:
      contents: write   # needed to push new_listings.json back to repo

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Run diff script
        run: python generate_new_listings.py

      - name: Commit & push new_listings.json
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/new_listings.json
          git diff --staged --quiet || git commit -m "chore: update new_listings.json [skip ci]"
          git push
